/** Enum for supported form field types in the dynamic form system */
export enum FormFieldType {
  Text = 'text',
  Number = 'number',
  Select = 'select',
  Checkbox = 'checkbox',
  Date = 'date',
  Textarea = 'textarea',
}
